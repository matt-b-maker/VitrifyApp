import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sorminsshack.glazevaultapp',
  appName: 'The Glaze Vault',
  webDir: 'www'
};

export default config;
