import { FilterSilicaPipe } from './filter-silica.pipe';

describe('FilterSilicaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSilicaPipe();
    expect(pipe).toBeTruthy();
  });
});
