import { FilterColorantPipe } from './filter-colorant.pipe';

describe('FilterColorantPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterColorantPipe();
    expect(pipe).toBeTruthy();
  });
});
