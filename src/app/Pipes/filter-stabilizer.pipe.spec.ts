import { FilterStabilizerPipe } from './filter-stabilizer.pipe';

describe('FilterStabilizerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStabilizerPipe();
    expect(pipe).toBeTruthy();
  });
});
