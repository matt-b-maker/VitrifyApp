import { FilterFluxPipe } from './filter-flux.pipe';

describe('FilterFluxPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFluxPipe();
    expect(pipe).toBeTruthy();
  });
});
